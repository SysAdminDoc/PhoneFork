<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- Catppuccin Mocha palette
         Source: https://catppuccin.com/palette/  Flavor: Mocha
         Hand-rolled; no `catppuccin/wpf` NuGet exists as of 2026-05.
         Foreground/Background setters that take a Brush MUST reference SolidColorBrush keys
         (the *Brush suffix), not the bare Color key — caught in OrganizeContacts v0.1.0. -->

    <!-- Backgrounds -->
    <Color x:Key="MochaBase">#FF1E1E2E</Color>
    <Color x:Key="MochaMantle">#FF181825</Color>
    <Color x:Key="MochaCrust">#FF11111B</Color>

    <!-- Surfaces -->
    <Color x:Key="MochaSurface0">#FF313244</Color>
    <Color x:Key="MochaSurface1">#FF45475A</Color>
    <Color x:Key="MochaSurface2">#FF585B70</Color>

    <!-- Overlays -->
    <Color x:Key="MochaOverlay0">#FF6C7086</Color>
    <Color x:Key="MochaOverlay1">#FF7F849C</Color>
    <Color x:Key="MochaOverlay2">#FF9399B2</Color>

    <!-- Text -->
    <Color x:Key="MochaText">#FFCDD6F4</Color>
    <Color x:Key="MochaSubtext1">#FFBAC2DE</Color>
    <Color x:Key="MochaSubtext0">#FFA6ADC8</Color>

    <!-- Accents -->
    <Color x:Key="MochaRosewater">#FFF5E0DC</Color>
    <Color x:Key="MochaFlamingo">#FFF2CDCD</Color>
    <Color x:Key="MochaPink">#FFF5C2E7</Color>
    <Color x:Key="MochaMauve">#FFCBA6F7</Color>
    <Color x:Key="MochaRed">#FFF38BA8</Color>
    <Color x:Key="MochaMaroon">#FFEBA0AC</Color>
    <Color x:Key="MochaPeach">#FFFAB387</Color>
    <Color x:Key="MochaYellow">#FFF9E2AF</Color>
    <Color x:Key="MochaGreen">#FFA6E3A1</Color>
    <Color x:Key="MochaTeal">#FF94E2D5</Color>
    <Color x:Key="MochaSky">#FF89DCEB</Color>
    <Color x:Key="MochaSapphire">#FF74C7EC</Color>
    <Color x:Key="MochaBlue">#FF89B4FA</Color>
    <Color x:Key="MochaLavender">#FFB4BEFE</Color>

    <!-- Brushes (always pair every Color with a SolidColorBrush of the same name + "Brush") -->
    <SolidColorBrush x:Key="MochaBaseBrush"      Color="{StaticResource MochaBase}"/>
    <SolidColorBrush x:Key="MochaMantleBrush"    Color="{StaticResource MochaMantle}"/>
    <SolidColorBrush x:Key="MochaCrustBrush"     Color="{StaticResource MochaCrust}"/>

    <SolidColorBrush x:Key="MochaSurface0Brush"  Color="{StaticResource MochaSurface0}"/>
    <SolidColorBrush x:Key="MochaSurface1Brush"  Color="{StaticResource MochaSurface1}"/>
    <SolidColorBrush x:Key="MochaSurface2Brush"  Color="{StaticResource MochaSurface2}"/>

    <SolidColorBrush x:Key="MochaOverlay0Brush"  Color="{StaticResource MochaOverlay0}"/>
    <SolidColorBrush x:Key="MochaOverlay1Brush"  Color="{StaticResource MochaOverlay1}"/>
    <SolidColorBrush x:Key="MochaOverlay2Brush"  Color="{StaticResource MochaOverlay2}"/>

    <SolidColorBrush x:Key="MochaTextBrush"      Color="{StaticResource MochaText}"/>
    <SolidColorBrush x:Key="MochaSubtext1Brush"  Color="{StaticResource MochaSubtext1}"/>
    <SolidColorBrush x:Key="MochaSubtext0Brush"  Color="{StaticResource MochaSubtext0}"/>

    <SolidColorBrush x:Key="MochaMauveBrush"     Color="{StaticResource MochaMauve}"/>
    <SolidColorBrush x:Key="MochaBlueBrush"      Color="{StaticResource MochaBlue}"/>
    <SolidColorBrush x:Key="MochaGreenBrush"     Color="{StaticResource MochaGreen}"/>
    <SolidColorBrush x:Key="MochaRedBrush"       Color="{StaticResource MochaRed}"/>
    <SolidColorBrush x:Key="MochaYellowBrush"    Color="{StaticResource MochaYellow}"/>
    <SolidColorBrush x:Key="MochaPeachBrush"     Color="{StaticResource MochaPeach}"/>
    <SolidColorBrush x:Key="MochaPinkBrush"      Color="{StaticResource MochaPink}"/>
    <SolidColorBrush x:Key="MochaTealBrush"      Color="{StaticResource MochaTeal}"/>
    <SolidColorBrush x:Key="MochaSapphireBrush"  Color="{StaticResource MochaSapphire}"/>
    <SolidColorBrush x:Key="MochaLavenderBrush"  Color="{StaticResource MochaLavender}"/>

    <!-- Convenience aliases used across PhoneFork views -->
    <SolidColorBrush x:Key="WindowBackgroundBrush" Color="{StaticResource MochaBase}"/>
    <SolidColorBrush x:Key="CardBackgroundBrush"   Color="{StaticResource MochaMantle}"/>
    <SolidColorBrush x:Key="CardBorderBrush"       Color="{StaticResource MochaSurface1}"/>
    <SolidColorBrush x:Key="AccentBrush"           Color="{StaticResource MochaMauve}"/>
    <SolidColorBrush x:Key="AccentSecondaryBrush"  Color="{StaticResource MochaBlue}"/>
    <SolidColorBrush x:Key="SuccessBrush"          Color="{StaticResource MochaGreen}"/>
    <SolidColorBrush x:Key="WarningBrush"          Color="{StaticResource MochaPeach}"/>
    <SolidColorBrush x:Key="DangerBrush"           Color="{StaticResource MochaRed}"/>
    <SolidColorBrush x:Key="ForegroundBrush"       Color="{StaticResource MochaText}"/>
    <SolidColorBrush x:Key="ForegroundMutedBrush"  Color="{StaticResource MochaSubtext0}"/>

    <!-- Global typography defaults -->
    <Style TargetType="{x:Type Window}">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="FontFamily" Value="Segoe UI"/>
        <Setter Property="FontSize" Value="13"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <Style TargetType="{x:Type TextBlock}">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="TextOptions.TextFormattingMode" Value="Ideal"/>
        <Setter Property="TextOptions.TextRenderingMode" Value="ClearType"/>
    </Style>

    <Style TargetType="{x:Type Label}">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
    </Style>

    <Style TargetType="{x:Type Button}">
        <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Padding" Value="12,7"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="MinWidth" Value="34"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <!-- Backdrop radius 8 — never use pill / 999 / capsule shapes. -->
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource MochaSurface1Brush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource MochaSurface2Brush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="AccentButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource MochaBaseBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type Button}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="8"
                            Padding="{TemplateBinding Padding}"
                            SnapsToDevicePixels="True">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                          RecognizesAccessKey="True"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource MochaLavenderBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource MochaLavenderBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource MochaTextBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsPressed" Value="True">
                            <Setter TargetName="border" Property="Background" Value="{StaticResource MochaBlueBrush}"/>
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource MochaBlueBrush}"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.5"/>
                            <Setter Property="Cursor" Value="Arrow"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style x:Key="DangerButton" TargetType="{x:Type Button}" BasedOn="{StaticResource {x:Type Button}}">
        <Setter Property="BorderBrush" Value="{StaticResource DangerBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource DangerBrush}"/>
    </Style>

    <Style TargetType="{x:Type TextBox}">
        <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CaretBrush" Value="{StaticResource AccentBrush}"/>
        <Setter Property="SelectionBrush" Value="{StaticResource MochaSurface2Brush}"/>
        <Setter Property="Padding" Value="9,6"/>
        <Setter Property="MinHeight" Value="34"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TextBox}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            CornerRadius="6">
                        <ScrollViewer x:Name="PART_ContentHost" Margin="0"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="border" Property="BorderThickness" Value="2"/>
                        </Trigger>
                        <Trigger Property="IsEnabled" Value="False">
                            <Setter Property="Opacity" Value="0.55"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type ComboBox}">
        <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="MinHeight" Value="34"/>
    </Style>

    <Style TargetType="{x:Type ComboBoxItem}">
        <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="Padding" Value="10,6"/>
    </Style>

    <Style TargetType="{x:Type CheckBox}">
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="MinHeight" Value="28"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
    </Style>

    <Style TargetType="{x:Type DataGrid}">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="RowBackground" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="AlternatingRowBackground" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="VerticalGridLinesBrush" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="SelectionUnit" Value="FullRow"/>
        <Setter Property="SelectionMode" Value="Extended"/>
        <Setter Property="RowHeaderWidth" Value="0"/>
        <Setter Property="AlternationCount" Value="2"/>
    </Style>

    <Style TargetType="{x:Type DataGridColumnHeader}">
        <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="BorderThickness" Value="0,0,1,1"/>
        <Setter Property="Padding" Value="8,6"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <Style TargetType="{x:Type DataGridRow}">
        <Setter Property="MinHeight" Value="30"/>
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
        <Style.Triggers>
            <Trigger Property="ItemsControl.AlternationIndex" Value="1">
                <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
            </Trigger>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource MochaSurface1Brush}"/>
            </Trigger>
            <Trigger Property="IsSelected" Value="True">
                <Setter Property="Background" Value="{StaticResource MochaSurface2Brush}"/>
                <Setter Property="Foreground" Value="{StaticResource MochaTextBrush}"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="{x:Type DataGridCell}">
        <Setter Property="BorderThickness" Value="0"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="VerticalContentAlignment" Value="Center"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type DataGridCell}">
                    <Border x:Name="cellBorder"
                            Background="{TemplateBinding Background}"
                            BorderBrush="{TemplateBinding BorderBrush}"
                            BorderThickness="{TemplateBinding BorderThickness}"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter VerticalAlignment="{TemplateBinding VerticalContentAlignment}"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsKeyboardFocusWithin" Value="True">
                            <Setter TargetName="cellBorder" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter TargetName="cellBorder" Property="BorderThickness" Value="1"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <Style TargetType="{x:Type TabControl}">
        <Setter Property="Background" Value="{StaticResource WindowBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
    </Style>

    <Style TargetType="{x:Type TabItem}">
        <Setter Property="Background" Value="Transparent"/>
        <Setter Property="Foreground" Value="{StaticResource ForegroundMutedBrush}"/>
        <Setter Property="Padding" Value="14,8"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type TabItem}">
                    <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            BorderBrush="Transparent"
                            BorderThickness="0,0,0,2"
                            Padding="{TemplateBinding Padding}">
                        <ContentPresenter ContentSource="Header" RecognizesAccessKey="True"
                                          HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                    <ControlTemplate.Triggers>
                        <Trigger Property="IsSelected" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsKeyboardFocused" Value="True">
                            <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource MochaBlueBrush}"/>
                            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                        </Trigger>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Foreground" Value="{StaticResource ForegroundBrush}"/>
                        </Trigger>
                    </ControlTemplate.Triggers>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

    <!-- Card-style container (backdrop radius 8 — never pill) -->
    <Style x:Key="Card" TargetType="{x:Type Border}">
        <Setter Property="Background" Value="{StaticResource CardBackgroundBrush}"/>
        <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="8"/>
        <Setter Property="Padding" Value="16"/>
    </Style>

    <Style x:Key="SubtleCard" TargetType="{x:Type Border}" BasedOn="{StaticResource Card}">
        <Setter Property="Background" Value="{StaticResource MochaSurface0Brush}"/>
        <Setter Property="Padding" Value="12"/>
    </Style>

    <Style TargetType="{x:Type ProgressBar}">
        <Setter Property="Height" Value="8"/>
        <Setter Property="Foreground" Value="{StaticResource AccentBrush}"/>
        <Setter Property="Background" Value="{StaticResource MochaSurface1Brush}"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="{x:Type ProgressBar}">
                    <Border Background="{TemplateBinding Background}" CornerRadius="4" SnapsToDevicePixels="True">
                        <Grid x:Name="PART_Track" ClipToBounds="True">
                            <Border x:Name="PART_Indicator"
                                    Background="{TemplateBinding Foreground}"
                                    CornerRadius="4"
                                    HorizontalAlignment="Left"/>
                        </Grid>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
    </Style>

</ResourceDictionary>
